name: create runner # workflow name
on: workflow_dispatch
jobs: 
  create-runner:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v6

      - name: list files
        run: ls -l